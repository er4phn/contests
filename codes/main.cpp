#include <bits/stdc++.h>
using namespace std;

using ll = long long;

#define sz(x) (int) (x).size()
#define mp make_pair

#ifdef DK
    #define eprintf(...) fprintf(stderr, __VA_ARGS__)
#else
    #define eprintf(...) 42
#endif

char tmp[2000100];

void scanf(string* s) {
    scanf("%s", tmp);
    *s = tmp;
}



int main() { 
    return 0;
}
